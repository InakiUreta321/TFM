#ifndef SCREEN_COMPONENT_H
#define SCREEN_COMPONENT_H
#include <stdint.h>  // para uint16_t, etc.

void drawComponentScreen();
void handleComponentScreenTouch(uint16_t x, uint16_t y);

#endif // SCREEN_COMPONENT_H
